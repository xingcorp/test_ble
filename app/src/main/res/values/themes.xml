<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- ========== NORDIC BEACON SCANNER THEME ========== -->
    <!-- Base application theme extending Material Design 3 -->
    
    <style name="Base.Theme.NordicBeaconScanner" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/nordic_blue</item>
        <item name="colorPrimaryVariant">@color/nordic_blue_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        
        <!-- Status bar -->
        <item name="android:statusBarColor">@color/nordic_blue_dark</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        
        <!-- Background colors -->
        <item name="android:windowBackground">@color/background_primary</item>
        <item name="colorSurface">@color/background_card</item>
        <item name="colorOnSurface">@color/text_primary</item>
        
        <!-- Error colors -->
        <item name="colorError">@color/error_red</item>
        <item name="colorOnError">@color/white</item>
    </style>

    <style name="Theme.NordicBeaconScanner" parent="Base.Theme.NordicBeaconScanner" />
    
    <!-- ========== CUSTOM COMPONENT STYLES ========== -->
    
    <!-- Beacon Detection Card Style -->
    <style name="BeaconCard">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">8dp</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">2dp</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">@color/divider</item>
    </style>
    
    <!-- Status Text Styles -->
    <style name="StatusText">
        <item name="android:textAppearance">?attr/textAppearanceHeadline6</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:gravity">center</item>
    </style>
    
    <style name="StatusText.Scanning" parent="StatusText">
        <item name="android:textColor">@color/success_green</item>
    </style>
    
    <style name="StatusText.Error" parent="StatusText">
        <item name="android:textColor">@color/error_red</item>
    </style>
    
    <!-- Button Styles -->
    <style name="PrimaryButton" parent="Widget.Material3.Button">
        <item name="android:layout_height">56dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/nordic_blue</item>
    </style>
    
    <style name="DangerButton" parent="Widget.Material3.Button">
        <item name="android:layout_height">56dp</item>
        <item name="android:textColor">@color/white</item>  
        <item name="backgroundTint">@color/error_red</item>
    </style>

</resources>
