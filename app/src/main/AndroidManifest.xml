<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========== BLUETOOTH PERMISSIONS ========== -->
    <!-- Legacy Bluetooth permissions (Android <12) -->
    <uses-permission android:name="android.permission.BLUETOOTH" 
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" 
        android:maxSdkVersion="30" />

    <!-- New Bluetooth permissions (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" 
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" 
        tools:targetApi="s" />

    <!-- ========== LOCATION PERMISSIONS ========== -->
    <!-- Required for BLE beacon scanning -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    
    <!-- Background location for continuous scanning when screen off -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- ========== SERVICE & BACKGROUND PERMISSIONS ========== -->
    <!-- Foreground service for continuous beacon scanning -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" 
        tools:targetApi="34" />

    <!-- Wake locks for CPU-intensive scanning operations -->  
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- System boot receiver for auto-restart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ========== HARDWARE REQUIREMENTS ========== -->  
    <!-- Require Bluetooth LE support -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <!-- Location services required for BLE scanning -->
    <uses-feature
        android:name="android.hardware.location"
        android:required="true" />

    <application
        android:name=".NordicBeaconApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NordicBeaconScanner"
        tools:targetApi="31">

        <!-- ========== MAIN ACTIVITY ========== -->
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NordicBeaconScanner"
            android:screenOrientation="portrait"
            android:launchMode="singleTop">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ========== ANALYTICS DASHBOARD ========== -->
        <activity
            android:name=".analytics.dashboard.AnalyticsDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.NordicBeaconScanner"
            android:screenOrientation="portrait"
            android:parentActivityName=".presentation.MainActivity">
            
            <!-- Parent activity meta-data for navigation -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.MainActivity" />
        </activity>

        <!-- ========== FOREGROUND SERVICES ========== -->
        
        <!-- Nordic Beacon Scanning Service -->
        <service
            android:name=".infrastructure.services.BeaconScanningService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"
            android:stopWithTask="false">
            
            <!-- Service can run independently of main app -->
            <meta-data
                android:name="service_description"
                android:value="Continuous Nordic beacon scanning service" />
        </service>

        <!-- ========== BROADCAST RECEIVERS ========== -->
        
        <!-- System Event Receiver for auto-restart capabilities -->
        <receiver
            android:name=".infrastructure.receivers.SystemEventReceiver"
            android:enabled="true"
            android:exported="true">
            
            <intent-filter android:priority="1000">
                <!-- Auto-start after device reboot -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                
                <!-- Auto-restart after app updates -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
                
                <!-- Battery optimization changes -->
                <action android:name="android.os.action.POWER_SAVE_MODE_CHANGED" />
                <action android:name="android.os.action.DEVICE_IDLE_MODE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- ========== WORK MANAGER BACKUP ========== -->
        
        <!-- Initialize WorkManager for backup scanning -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            
            <!-- WorkManager Initialization -->
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- ========== PERFORMANCE OPTIMIZATIONS ========== -->
        
        <!-- Disable unnecessary Android components -->
        <receiver android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:permission="android.permission.DUMP"
            android:exported="true"
            tools:node="remove" />

    </application>

    <!-- ========== SECURITY CONFIGURATIONS ========== -->
    
    <!-- Network security config (if needed for future features) -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Prevent backup of sensitive beacon data -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" 
        android:maxSdkVersion="28" />

</manifest>
